#Testproject for `CoreEnvironment` of the IntelliJ Framework

In short:

- clone this project
- open it with Community Edition of IDEA
- use java 1.6 for compilation
- run `Main` in the `simple-parser` module